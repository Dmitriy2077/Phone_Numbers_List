<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mingaz.lubinskiy.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mingaz_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mingaz_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.DepartmentsEmployeesListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.EmployeesListActivity"
            android:exported="false" />
        <activity
            android:name=".ui.activities.EmployeeInfoActivity"
            android:exported="false" />
    </application>
</manifest>
